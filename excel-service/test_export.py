import requests

url = "http://127.0.0.1:8003/api/export/kc"
# But wait, the endpoint expects a huge JSON payload which I don't have.
# Let's just look at the logs directly or run a script that imports data.
